{
  "compounds": [],
  "configurations": [
    {

      "type": "node",
      "name": "invoke-js-file",
      "request": "launch",
      "program": "${workspaceFolder}/${relativeFile}",
      "envFile": "${workspaceFolder}/.env",
      "cwd": "${workspaceFolder}",
    },
  ]
}